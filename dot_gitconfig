[user]
  name = Spedon Wen
  email = realsped0n@outlook.com
  username = Sped0n

[core]
  excludesfile = /Users/spedon/.gitignore_global

[commit]
  template = /Users/spedon/.config/git/template/.git-commit-template.txt
	cleanup = strip

[core]
  editor = nvim
  pager = delta

[interactive]
  diffFilter = delta --color-only

[delta]
  navigate = true    # use n and N to move between diff sections
  light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)
  side-by-side = true
  line-numbers = true

[merge]
  conflictstyle = diff3

[diff]
  colorMoved = default
